# E2E Test Configuration
# Copy this file to .env and set your KSP installation path

# Required: Path to Kerbal Space Program installation
KSP_DIR=/path/to/Kerbal Space Program

# Optional: Override auto-detected paths
# PLAYER_LOG=/path/to/Player.log
# KSP_APP=/path/to/KSP.app

# Optional: Disable ksp-mcp workarounds for testing raw MechJeb behavior
# Workarounds include: course correction 3x multiplier, node half-burn shift, crash avoidance
# WORKAROUNDS_ENABLED=false
